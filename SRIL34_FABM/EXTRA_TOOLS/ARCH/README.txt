The arch files are added in this directory for completness, 
however they should already be downloaded when running the 'clone_repositories.sh'. 

the scripts to compile nemo and xios are set to cp the arch files from those repostories, not this one. 
 
